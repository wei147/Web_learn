import { createStore } from 'vuex'

// VueX 数据管理框架
// Vuex 创建了一个全局唯一的仓库,用来存放全局的数据
export default createStore({
  state: {
    name:"wei"
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
